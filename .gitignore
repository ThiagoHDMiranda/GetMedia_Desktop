# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
.DS_Store
*.pid
*.pid.lock

# Coverage
coverage
*.lcov
.nyc_output

# Dependencies
node_modules/

# TypeScript
*.tsbuildinfo

# Env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vite (build output)
dist/

# Electron
dist-electron/
release/

# Cache
.cache
.npm
.eslintcache

# OS
Thumbs.db

# Binaries — Windows .exe files committed for CI builds (electron-builder needs them)
# ffmpeg.exe   (~87MB) — bundled as extraResource in the Windows installer
# yt-dlp.exe   (~18MB) — bundled as extraResource in the Windows installer

# Large archives (extracted binaries are committed directly)
*.tar.xz

# Tasks and TODOs
tasks.md
TODO.md